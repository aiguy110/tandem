{
  "name": "tandem-browser-spike",
  "private": true,
  "type": "module",
  "description": "De-risk the shared browser: agent drives via CDP while a human views (screencast) and grabs the wheel (hard-pause token). Steel wraps this same CDP surface.",
  "scripts": {
    "derisk": "tsx src/derisk.ts",
    "broker": "tsx src/broker.ts"
  },
  "dependencies": {
    "playwright-core": "^1.48.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.2"
  }
}
